# Performance change log

Append a row when a perf-focused PR merges. Copy the `compare.py` summary into the notes column.

| Date (UTC) | PR / branch | vs baseline | Notes |
|------------|-------------|-------------|-------|
| _example_ | `perf/baseline-record` | — | Initial `run_suite.py` harness added. |
